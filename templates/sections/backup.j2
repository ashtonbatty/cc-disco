  # ===========================================================================
  # Backup and Monitoring Agents
  # ===========================================================================
  agents_detected:
{% if _command_heavy %}
{% for agent in hv.disco_backup_monitoring.stdout_lines | default([]) %}
{% if agent | trim | length > 0 %}
    - "{{ agent | trim }}"
{% endif %}
{% else %}
    []
{% endfor %}
{% else %}
    []  # skipped (collect_command_heavy_probes=false)
{% endif %}
